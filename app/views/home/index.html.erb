<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deals Page</title>
    <link rel="stylesheet" href="style.css">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f7fc;
            color: #333;
            line-height: 1.6;
        }

        /* Carousel Styles */
        .carousel-container {
            width: 100%;
            overflow: hidden;
            position: relative;
        }

        .carousel-item img {
            width: 100%;
            object-fit: cover;
            border-radius: 10px;
        }

        .carousel-inner {
            display: flex;
            transition: transform 0.5s ease-in-out;
        }

        /* Deals Section */
        main {
            padding: 40px 20px;
            max-width: 1200px;
            margin: auto;
        }

        h2 {
            text-align: center;
            font-size: 32px;
            color: #3b5998;
            margin-bottom: 30px;
            position: relative;
        }

        h2::after {
            content: '';
            display: block;
            width: 80px;
            height: 3px;
            background-color: #3b5998;
            margin: 10px auto;
        }

        .deals {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            gap: 20px;
        }

        .deal-card {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: box-shadow 0.3s ease;
            text-align: center;
            flex-basis: calc(33.33% - 20px);
            max-width: calc(33.33% - 20px);
            padding-bottom: 20px;
        }

        .deal-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .deal-card:hover {
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }

        .deal-card h3 {
            font-size: 20px;
            color: #333;
            margin: 15px 0;
        }

        .deal-card p {
            color: #777;
            font-size: 14px;
            margin-bottom: 15px;
        }

        .deal-card button {
            background-color: #3b5998;
            color: #fff;
            border: none;
            padding: 10px 25px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .deal-card button:hover {
            background-color: #2a4377;
        }

        /* Media Queries */
        @media (max-width: 768px) {
            .deal-card {
                flex-basis: calc(50% - 20px);
                max-width: calc(50% - 20px);
            }
        }

        @media (max-width: 600px) {
            .deal-card {
                flex-basis: calc(100% - 20px);
                max-width: calc(100% - 20px);
            }
        }
    </style>
</head>
<body>
<div class="carousel-container">
    <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img src="<%= asset_path ('image1.png') %>" alt="First slide">
            </div>
            <div class="carousel-item">
                <img src="<%= asset_path ('image2.png') %>" alt="Second slide">
            </div>
            <div class="carousel-item">
                <img src="<%= asset_path ('image3.png') %>" alt="Third slide">
            </div>
            <div class="carousel-item">
                <img src="<%= asset_path ('image5.png') %>" alt="Third slide">
            </div>
            <div class="carousel-item">
                <img src="<%= asset_path ('image6.png') %>" alt="Third slide">
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
</div>

<main>
    <h2>All Categories</h2>
    <section class="deals">
        <% @categories.each do |category| %>
        <div class="deal-card">
            <% if category.image.attached? %>
            <a href="<%= category_products_path(category) %>">
                <%= image_tag(category.image, class: "img-fluid", alt: category.c_name) %>
            </a>
            <% end %>
            <h3><%= category.c_name %></h3>
        </div>
        <% end %>
    </section>
</main>

<script src="script.js"></script>
</body>
</html>
